#!/usr/bin/env node

/**
 * Validate world template YAML files against the schema.
 * Full implementation in Phase 1.
 */

// TODO: Load all YAML files from packages/config/src/templates/
// TODO: Validate each against the world template zod schema
// TODO: Report errors with file paths and line numbers

console.log('validate-templates: No templates to validate yet (Phase 1)');
process.exit(0);
