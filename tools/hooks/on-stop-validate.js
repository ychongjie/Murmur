#!/usr/bin/env node

/**
 * Stop hook: Final validation before Claude finishes.
 *
 * Runs lightweight checks to catch any remaining issues.
 * Currently a placeholder — full implementation will run
 * typecheck and lint as the project matures.
 *
 * Exit codes:
 *   0 = all good
 *   2 = issues found, Claude should continue fixing
 */

// Placeholder: will add typecheck + lint validation once
// dependencies are installed and the project is buildable
process.exit(0);
