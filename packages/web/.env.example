# Backend API base URL (used by server-side data fetching)
NEXT_PUBLIC_API_URL=http://localhost:3001

# WebSocket base URL (used by client-side real-time connection)
NEXT_PUBLIC_WS_URL=ws://localhost:3001
